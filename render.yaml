# Agentbot Render Blueprint

services:
  # PostgreSQL
  - type: pserv
    name: agentbot-postgres
    plan: starter
    region: oregon

  # Redis
  - type: redis
    name: agentbot-redis
    plan: starter
    region: oregon

  # Backend API - Agentbot provisioning
  - type: web
    name: agentbot-api
    env: docker
    dockerfilePath: agentbot-backend/Dockerfile
    rootDir: agentbot-backend
    plan: starter
    region: oregon
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PORT
        value: "3001"
      - key: NODE_ENV
        value: production
      - key: ALLOWED_ORIGINS
        value: https://agentbot-api.onrender.com

  # Worker
  - type: worker
    name: agentbot-worker
    env: docker
    dockerfilePath: agentbot-worker/Dockerfile
    rootDir: agentbot-worker
    plan: starter
    region: oregon
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
