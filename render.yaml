services:
  - type: web
    name: agentbot-api
    runtime: docker
    dockerfilePath: agentbot-backend/Dockerfile.prod
    healthCheckPath: /health
    autoDeploy: true

  - type: worker
    name: agentbot-worker
    runtime: docker
    dockerfilePath: agentbot-worker/Dockerfile.prod
    autoDeploy: true

databases:
  - name: agentbot-postgres
