/**
 * Deprecated shim — the Geist-style primitives now live in the vercel/ui-derived
 * kit at app/components/ui. Import from there instead:
 *
 *   import { Spinner, LoadingDots } from '@/app/components/ui'
 */
export { Spinner, LoadingDots } from '@/app/components/ui'
