# Dockerfile.openclaw — Agentbot agent runtime on Fly.io
FROM node:22-slim

RUN npm install -g openclaw@2026.5.28

RUN mkdir -p /root/.openclaw/workspace

COPY openclaw.json /root/.openclaw/openclaw.json
COPY entrypoint-openclaw.sh /entrypoint.sh

EXPOSE 18789

CMD ["/entrypoint.sh"]
