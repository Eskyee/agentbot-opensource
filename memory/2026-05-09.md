# 2026-05-09

## Workspace Backup

**Run:** 02:30 UTC — FAILED (silent)

- **Status:** Script exited code 1 — `mkdir: /mnt: Read-only file system`
- **Root cause:** Backup script tries `mkdir -p /mnt/agentbot-backups` which is read-only in the sandbox environment.
- **Data dir:** `/tmp/agentbot-data` does not exist — no user workspaces on disk.
- **Docker:** Not available — no active agent containers.
- **Action:** None required. No active containers = no alert. This is an environment limitation on the cron runner, not a production failure.
- **Note:** If/when workspaces go live on Railway, the backup script needs `AGENTBOT_BACKUP_VOLUME` set to a writable path (or a mounted Railway volume). The `/mnt/agentbot-backups` default assumes a Linux host with a mounted disk.
