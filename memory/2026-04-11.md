
## Open Source Repo Split Strategy (04:23)

**Two-repo model:**
1. `agentbot-opensource` — public ecosystem layer (SDK, agent spec, plugins, examples, docs, CLI)
2. `agentbot` — private infra (hosted control plane, billing, auth, secrets, tenancy, premium features)

**Framing:** "Agentbot is open-source infrastructure for AI workers. Agentbot Cloud is the managed deployment platform."

**Build order:** Agent Spec → CLI → Plugin Contract → Example Agents → Hosted Upgrade Path

**Moat:** NOT secrecy. Moat = best hosted experience + fastest deploy + best UX + best docs + strongest community.

**Repo tree for ChatGPT:** `memory/repo-tree-for-chatgpt.md`
**Full split notes:** `memory/open-source-repo-split.md`

## Agentbot SDK Created (04:32)

**Location:** `/Users/raveculture/agentbot-sdk`
**Status:** Git initialized, initial commit done (d4ce49b)
**Files:** 19 files, 984 lines
**Compiles:** ✅ Zero TypeScript errors
**Leaks:** Zero proprietary code

### What's in it:
- Agent definition format (Markdown + YAML frontmatter spec)
- Agent parser (clean-room implementation)
- Orchestration engine (tool classifier, batch partitioner, concurrent executor)
- 4 example agents (research, outreach, content, multi-agent workflow)
- README, CONTRIBUTING, MIT LICENSE

### What's NOT in it:
- No billing, auth, tenancy, keys
- No moltx, engage-moltx, baseFM, Bankr
- No internal roles (CEO, CFO, CTO)
- No WebSocket handlers, backend integrations
- No x402, BTCPay

### Next steps:
- Push to GitHub (need to create repo or decide on naming)
- Decide: new repo `agentbot-sdk` or replace `agentbot-opensource` content
- CLI stub (`packages/cli/`)
- Plugin examples (`packages/plugins/`)

## SDK Pushed to GitHub (04:38)

**Repo:** https://github.com/Eskyee/agentbot-sdk
**Status:** PUBLIC, 3 commits, live
**Framing:** "Docker for AI workers"
**Existing repo:** agentbot-opensource untouched (people already linked)

### What's public:
- Agent definition format (Markdown + YAML spec)
- Agent parser + orchestration engine
- CLI (init, run, dev/stub)
- Tool definition interface
- 4 example agents
- README, CONTRIBUTING, MIT LICENSE

### What's NOT public (stays private):
- Billing, auth, tenancy, keys
- moltx, baseFM, Bankr integrations
- Internal roles, WebSocket handlers
- x402, BTCPay

### Key decisions:
- "Docker for AI workers" framing — instant dev understanding
- Separate repo from agentbot-opensource (don't break existing links)
- Clean-room implementation (no proprietary code copied)
- TODO updated with extraction plan and build order
