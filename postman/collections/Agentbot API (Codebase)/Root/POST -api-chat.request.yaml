$kind: http-request
name: POST /api//chat
url: "{{baseUrl}}/api//chat"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 48000
