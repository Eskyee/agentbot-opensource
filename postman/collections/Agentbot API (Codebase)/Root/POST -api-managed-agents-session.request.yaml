$kind: http-request
name: POST /api//managed-agents/session
url: "{{baseUrl}}/api//managed-agents/session"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 415000
