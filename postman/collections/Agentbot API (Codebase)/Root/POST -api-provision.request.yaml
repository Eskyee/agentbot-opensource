$kind: http-request
name: POST /api//provision
url: "{{baseUrl}}/api//provision"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 3000
