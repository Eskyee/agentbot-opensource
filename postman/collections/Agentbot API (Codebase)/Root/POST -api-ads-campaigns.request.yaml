$kind: http-request
name: POST /api//ads/campaigns
url: "{{baseUrl}}/api//ads/campaigns"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 15000
