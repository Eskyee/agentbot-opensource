$kind: http-request
name: POST /api//stripe/credits
url: "{{baseUrl}}/api//stripe/credits"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 529000
