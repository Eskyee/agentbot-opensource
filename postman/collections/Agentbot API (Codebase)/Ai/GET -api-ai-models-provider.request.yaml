$kind: http-request
name: GET /api/ai/models/:provider
url: "{{baseUrl}}/api/ai/models/:provider"
method: GET
headers:
  Cookie: next-auth.session-token={{sessionToken}}
pathVariables:
  provider: ""
order: 3000
