$kind: http-request
name: POST /api/agents/:id/update
url: "{{baseUrl}}/api/agents/:id/update"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 12000
