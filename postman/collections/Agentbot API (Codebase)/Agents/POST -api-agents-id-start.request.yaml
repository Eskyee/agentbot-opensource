$kind: http-request
name: POST /api/agents/:id/start
url: "{{baseUrl}}/api/agents/:id/start"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 9000
