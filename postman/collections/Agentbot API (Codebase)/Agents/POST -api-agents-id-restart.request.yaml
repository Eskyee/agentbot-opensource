$kind: http-request
name: POST /api/agents/:id/restart
url: "{{baseUrl}}/api/agents/:id/restart"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 11000
