$kind: http-request
name: POST /api/registration/heartbeat
url: "{{baseUrl}}/api/registration/heartbeat"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 5000
