$kind: http-request
name: POST /api/underground/bitcoin/wallets
url: "{{baseUrl}}/api/underground/bitcoin/wallets"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 10000
