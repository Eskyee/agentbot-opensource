$kind: http-request
name: POST /api/underground/wallets
url: "{{baseUrl}}/api/underground/wallets"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 4000
