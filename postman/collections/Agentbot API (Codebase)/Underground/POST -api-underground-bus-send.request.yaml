$kind: http-request
name: POST /api/underground/bus/send
url: "{{baseUrl}}/api/underground/bus/send"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 1000
