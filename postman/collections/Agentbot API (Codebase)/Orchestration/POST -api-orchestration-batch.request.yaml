$kind: http-request
name: POST /api/orchestration/batch
url: "{{baseUrl}}/api/orchestration/batch"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 1000
