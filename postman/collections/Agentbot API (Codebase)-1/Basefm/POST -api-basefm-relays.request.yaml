$kind: http-request
name: POST /api/basefm/relays
url: "{{baseUrl}}/api/basefm/relays"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 2000
