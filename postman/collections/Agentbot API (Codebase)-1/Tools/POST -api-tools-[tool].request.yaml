$kind: http-request
name: POST /api/tools/[tool]
url: "{{baseUrl}}/api/tools/[tool]"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 2000
