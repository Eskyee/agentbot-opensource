$kind: http-request
name: POST /api/community/governance
url: "{{baseUrl}}/api/community/governance"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 2000
