$kind: http-request
name: POST /api/ai/estimate-cost
url: "{{baseUrl}}/api/ai/estimate-cost"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 6000
