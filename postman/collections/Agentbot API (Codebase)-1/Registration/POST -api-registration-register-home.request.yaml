$kind: http-request
name: POST /api/registration/register-home
url: "{{baseUrl}}/api/registration/register-home"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 3000
