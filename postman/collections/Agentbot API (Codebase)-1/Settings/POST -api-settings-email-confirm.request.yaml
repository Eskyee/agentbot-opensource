$kind: http-request
name: POST /api/settings/email/confirm
url: "{{baseUrl}}/api/settings/email/confirm"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 7000
