$kind: http-request
name: POST /api/wallet/sessions
url: "{{baseUrl}}/api/wallet/sessions"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 6000
