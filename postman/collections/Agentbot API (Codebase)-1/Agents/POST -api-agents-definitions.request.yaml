$kind: http-request
name: POST /api/agents/definitions
url: "{{baseUrl}}/api/agents/definitions"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 41000
