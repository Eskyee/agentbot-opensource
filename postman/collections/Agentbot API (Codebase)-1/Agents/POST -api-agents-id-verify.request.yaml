$kind: http-request
name: POST /api/agents/:id/verify
url: "{{baseUrl}}/api/agents/:id/verify"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
pathVariables:
  id: ""
body:
  type: json
  content: "{}"
order: 30000
