$kind: http-request
name: POST /api/social/reports
url: "{{baseUrl}}/api/social/reports"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 38000
