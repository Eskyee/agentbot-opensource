$kind: http-request
name: POST /api/social/comments/[id]/vote
url: "{{baseUrl}}/api/social/comments/[id]/vote"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 1000
