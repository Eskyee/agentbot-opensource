$kind: http-request
name: POST /api/underground/events
url: "{{baseUrl}}/api/underground/events"
method: POST
headers:
  Cookie: next-auth.session-token={{sessionToken}}
  Content-Type: application/json
body:
  type: json
  content: "{}"
order: 3000
