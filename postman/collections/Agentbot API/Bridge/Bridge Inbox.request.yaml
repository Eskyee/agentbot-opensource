$kind: http-request
url: "{{baseUrl}}/api/bridge/inbox?reader=atlas-agentbot&channel=general"
method: GET
headers:
  X-Bridge-Secret: "{{bridgeSecret}}"
queryParams:
  reader: atlas-agentbot
  channel: general
order: 5000
