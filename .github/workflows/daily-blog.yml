name: Daily Blog Post

# Disabled — requires content generation setup
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-blog-post:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled — re-enable when blog content pipeline is ready"
