name: Deployment Flow Test

# Disabled — requires live API secrets not available in CI
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled — requires API secrets"
