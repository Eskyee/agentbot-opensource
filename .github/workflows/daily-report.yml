name: Daily Report

# Disabled — email secrets not configured
on:
  workflow_dispatch:

jobs:
  daily-report:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled — configure MAIL_USERNAME/MAIL_PASSWORD secrets to enable"
