name: OpenClaw Agent Automation

# Disabled — requires OPENCLAW_TOKEN/URL secrets
on:
  workflow_dispatch:

jobs:
  automate:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: echo "Disabled — configure OPENCLAW_TOKEN and OPENCLAW_URL secrets to enable"
