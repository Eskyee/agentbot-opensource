name: Contract Tests

# Disabled — no Foundry project in this repo.
# Re-enable when smart contracts are added.
on:
  workflow_dispatch:

jobs:
  check:
    name: Foundry project
    runs-on: ubuntu-latest
    if: false  # Disabled
    steps:
      - uses: actions/checkout@v5
      - run: echo "Foundry tests disabled — no contracts in this repo."
