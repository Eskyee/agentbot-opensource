// Mock for @coinbase/agentkit-vercel-ai-sdk (ESM-only chain via @coinbase/agentkit).
// See agentkit.ts mock for rationale.

export function getVercelAITools(): Record<string, unknown> {
  return {};
}
