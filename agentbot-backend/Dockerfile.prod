FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install --production

# Build
COPY . .
RUN npm run build

# Listen on all interfaces (Render handles this)
EXPOSE 3001

CMD ["node", "dist/index.js"]
